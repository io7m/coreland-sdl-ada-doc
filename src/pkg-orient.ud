(section
  (title "Orientation")

  (subsection
    (title "Overview")

    (para "This package provides a set of Ada bindings to "
      (link-ext "http://libsdl.org" "SDL") "."))
) ; end of section
